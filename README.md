\# Build

mvn clean install

\# Chạy project

mvn spring-boot:run

\# Build

npm install

npm run dev

docker compose up --build -d

docker build -t myapp-frontend .

docker build -t myapp-backend .



